<nav id="mobile-nav">
<!--   <% for (var i in theme.menu){ %>
    <a href="<%- url_for(theme.menu[i]) %>" class="mobile-nav-link"><%= i %></a>
  <% } %> -->
  <% site.categories.sort('name').each(function(item) { %>
    <a href="<%- url_for(config.category_dir + '/' + item.name) %>" class="mobile-nav-link"><%= item.name %></a>
  <% }) %>
</nav>